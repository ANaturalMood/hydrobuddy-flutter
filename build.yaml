targets:
  $default:
    builders:
      drift_dev:
        options:
          store_date_time_values_as_text: true
      json_serializable:
        options:
          checked: true
      freezed:
        options:
          union_key: type
    auto_apply_builders: true
